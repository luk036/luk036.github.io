{
  "name": "luk036.github.io",
  "version": "1.0.0",
  "description": "Static GitHub Pages site with educational content",
  "scripts": {
    "svgo": "svgo --folder .",
    "precommit": "pre-commit run --all-files",
    "build": "node scripts/build.js",
    "check-links": "node scripts/check-links.js"
  }
}
